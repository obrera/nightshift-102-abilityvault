services:
  abilityvault:
    build: .
    environment:
      PORT: "3000"
      PUBLIC_BASE_URL: "https://abilityvault102.colmena.dev"
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
    name: dokploy-network
